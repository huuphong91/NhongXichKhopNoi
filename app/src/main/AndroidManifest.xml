<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.phongtoanhuu.nhongxichkhopnoi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TAITRONGLUC"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.sprocket.SprocketActivity"></activity>
        <activity android:name=".ui.rollerchain.RollerChainActivity" />
        <activity android:name=".ui.coupling.FclCouplingActivity" />
        <activity android:name=".ui.coupling.LCouplingActivity" />
        <activity android:name=".ui.coupling.NmCouplingActivity" />
        <activity android:name=".ui.coupling.KcCouplingActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>